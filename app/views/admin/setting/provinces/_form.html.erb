<%= form_for(@admin_setting_province) do |f| %>
  <% if @admin_setting_province.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@admin_setting_province.errors.count, "error") %> prohibited this admin_setting_province from being saved:</h2>

      <ul>
      <% @admin_setting_province.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :en %><br>
    <%= f.text_field :en %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
