<%- model_class = CaseUser -%>
<div class="page-header">
  <h1>主要案例</h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th><%= model_class.human_attribute_name(:id) %></th>
      <th>姓名</th>
      <th>职位</th>
      <th>公司</th>
      <th>头像</th>
     
      <th>语言</th>
      <th><%= model_class.human_attribute_name(:active) %></th>
      <th>创建时间</th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @case_users.each do |case_user| %>
      <tr>
        <td><%= link_to case_user.id, case_user_path(case_user) %></td>
        <td><%= case_user.name %></td>
        <td><%= case_user.title %></td>
        <td><%= case_user.company %></td>
        <td><img src="<%= case_user.img %>"></td>
      
        <td><%= case_user.lang %></td>
        <td><%= case_user.active %></td>
        <td><%=l case_user.created_at %></td>
        <td>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_case_user_path(case_user), :class => 'btn btn-default btn-xs' %>
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      case_user_path(case_user),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-xs btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_case_user_path,
            :class => 'btn btn-primary' %>
